---
workloads:
  test-service:
    component: web
    container:
      image:
        repository: test-repo/test-image
        tag: 1.0.0
      volumes:
        - name: test-service-configmap
          type: configMap
          path: /data/configmap
          readOnly: true

configMaps:
  test-service-configmap:
    data:
      config.json: |
        {
          "key": "value",
          "number": 42
        }
      settings.txt: |
        FOO=bar
        BAZ=qux
