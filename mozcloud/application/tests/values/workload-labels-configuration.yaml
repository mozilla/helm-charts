---
# Overlay applied on top of values/basic-configuration.yaml. Adds a custom
# label to test-service to confirm workloads.<name>.labels propagates to both
# the Deployment and its Pod template.
workloads:
  test-service:
    labels:
      registry.gke.io/functional-type: AGENT
