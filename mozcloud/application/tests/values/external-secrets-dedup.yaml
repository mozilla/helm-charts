---
externalSecrets:
  default:
    enabled: true

workloads:
  test-service:
    component: web
    containers:
      app:
        image:
          repository: test-repo/test-image
          tag: 1.0.0
        # Listing the default secret explicitly should not cause a duplicate
        secrets:
          - test-chart-secrets
