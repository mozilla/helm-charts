---
workloads:
  test-service:
    component: web
    containers:
      app:
        image:
          repository: test-repo/test-image
          tag: 1.0.0
    hosts:
      # Host using a shared gateway (like preview environments)
      shared-gateway-host:
        type: external
        domains:
          - shared.test-domain.com
        sharedGateway:
          name: sandbox-high-preview-gateway
          namespace: preview-shared-infrastructure
