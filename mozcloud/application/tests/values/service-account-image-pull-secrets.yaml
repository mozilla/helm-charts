---
serviceAccounts:
  default:
    imagePullSecrets:
      - default-registry-secret
  custom-sa:
    imagePullSecrets:
      - custom-registry-secret-1
      - custom-registry-secret-2

workloads:
  web:
    component: web
    containers:
      app:
        image:
          repository: test-repo/test-image
          tag: 1.0.0
