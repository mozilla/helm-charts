---
podMonitorings:
  telegraf:
    match:
      labels:
        app.kubernetes.io/name: telegraf
    port: 8080
  app-metrics:
    match:
      workload: test-service
  legacy-metrics:
    match:
      labels:
        app.kubernetes.io/name: legacy-exporter
    port: 9100
    nameOverride: legacy-job-name
workloads:
  test-service:
    component: web
    containers:
      app:
        port: 9090
        image:
          repository: test-repo/test-image
          tag: 1.0.0
