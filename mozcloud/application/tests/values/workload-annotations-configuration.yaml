---
# Overlay applied on top of values/basic-configuration.yaml. Adds a custom
# annotation to test-service to confirm workloads.<name>.annotations is
# accepted by the schema and applied to both the Deployment and its Pod
# template.
workloads:
  test-service:
    annotations:
      iam.gke.io/spiffe-identity-type: agent-identity
