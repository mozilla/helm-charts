apiVersion: v1
kind: Service
metadata:
  name: {{ include "jameslabel.fullname" . }}
  labels:
    {{- include "jameslabel.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jameslabel.selectorLabels" . | nindent 4 }}
