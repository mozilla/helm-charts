---
ingresses:
  test-ingress:
    staticIpName: mozcloud-test-dev-ip-v4
    hosts:
      - domains:
          - test-service.dev.test-domain.com
        tls:
          createCertificates: false
        paths:
          - path: /
            backend:
              service:
                port: 8080
