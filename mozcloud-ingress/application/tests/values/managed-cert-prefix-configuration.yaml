---
ingresses:
  test-ingress:
    staticIpName: mozcloud-test-dev-ip-v4
    hosts:
      - domains:
          - test-service.dev.test-domain.com
        tls:
          type: ManagedCertificate
          prefix: mcrt
        paths:
          - path: /
            backend:
              service:
                port: 8080
