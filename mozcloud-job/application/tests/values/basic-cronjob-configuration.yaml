---
cronJobs:
  test-cronjob:
    config:
      schedule: '0 * * * *'  # can also be '@hourly'
    container:
      image:
        repository: test-repo/test-image
        tag: latest
      command: ["sh", "-c"]
      args: ["echo", "foo", "bar"]
