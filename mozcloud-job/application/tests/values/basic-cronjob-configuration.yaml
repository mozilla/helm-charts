---
cronJobs:
  test-cronjob:
    config:
      schedule: '0 * * * *'  # can also be '@hourly'
    containers:
      - name: app
        image: test-repo/test-image
        command: ["sh", "-c"]
        args: ["echo", "foo", "bar"]
